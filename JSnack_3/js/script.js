// TEXT JSnack1
// Scrivere una funzione contaElementi() che, dato un array in input, restituisca il numero di elementi presenti nell'array

// DATA COLLECTION

// Define constant of given array (inputArray).

const inputArray = ["Sea", "Moon", "Sky", "Stars"];

console.log("inputArray", inputArray, typeof inputArray);

const elementsArrayNum = contaElementi(inputArray);

console.log("Number of elements in the array", elementsArrayNum, typeof elementsArrayNum);
