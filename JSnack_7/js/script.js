// TEXT JSnack1
// Scrivere una funzione rimuoviDallaCoda() che, dato un array A in input, restituisca un nuovo array B, con tutti gli elementi dell'array A, tranne l'ultimo.

// DATA COLLECTION

// Define constant of given array (inputArray).

const inputArray = ["Sea", "Moon", "Sky", "Stars"];

console.log("inputArray", inputArray, typeof inputArray);

const newArray = removeFromStart(inputArray);

console.log("newArray", newArray, typeof newArray);